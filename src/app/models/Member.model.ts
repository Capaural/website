
export class Member {
    photo: string;
    constructor(public name: string, public lastName: string, public alias: string, public grade: string, public pole: string, public description: string) {}
}